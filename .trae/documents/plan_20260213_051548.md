Saya telah menganalisis kode Anda dan menemukan bahwa logika "Dark Mode" saat ini terduplikasi di beberapa tempat (`App.tsx`, `PublicNavbar.tsx`, `Header.tsx`), yang menyebabkan ketidakkonsistenan dan kode yang sulit dirawat. Selain itu, transisi antar mode belum mulus (kurang profesional).

Berikut adalah rencana perbaikan menyeluruh untuk membuat website Anda tampak lebih profesional dan fungsional:

### 1. Sentralisasi Logika Tema (Theme Context)
Saya akan membuat `ThemeContext` agar status gelap/terang dikelola di satu tempat saja. Ini menjamin sinkronisasi sempurna antara halaman publik dan dashboard.
*   **File Baru**: `src/contexts/ThemeContext.tsx`
*   **Fitur**: Otomatis mendeteksi preferensi sistem, menyimpan pilihan user, dan sinkronisasi realtime.

### 2. Refactoring Komponen
Saya akan membersihkan kode duplikat dan menghubungkan komponen ke `ThemeContext` baru.
*   **`src/App.tsx`**: Menghapus logic manual dan membungkus aplikasi dengan `ThemeProvider`.
*   **`src/components/PublicNavbar.tsx` & `src/components/Header.tsx`**: Mengubah tombol toggle agar menggunakan context global.

### 3. Peningkatan CSS & Tampilan Profesional
Untuk menjawab keluhan "belum maksimal", saya akan meningkatkan CSS di `src/index.css`:
*   **Transisi Halus**: Menambahkan properti `transition-colors duration-300` agar perubahan warna tidak kaget/kedip.
*   **Custom Scrollbar**: Mengubah tampilan scrollbar browser agar sesuai dengan tema (gelap/terang).
*   **Animasi Ikon**: Menambahkan animasi rotasi saat tombol matahari/bulan diklik.
*   **Konsistensi Warna**: Memastikan warna background dan teks memiliki kontras yang nyaman di mata (tidak terlalu hitam pekat atau putih menyilaukan).

### 4. Verifikasi Halaman Register
Saya akan memastikan halaman `Register.tsx` yang baru kita ubah menggunakan variabel tema yang benar agar input form dan teks terlihat jelas di kedua mode.

Apakah Anda setuju dengan rencana perbaikan struktur dan tampilan ini?